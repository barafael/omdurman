[package]
name = "traceability-typst"
description = "Generate a Typst traceability matrix from traceability.toml"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
